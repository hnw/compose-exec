# Hello, Pandoc

This Markdown file is converted to HTML by the **pandoc** Compose service.

* The controller never installs Pandoc itself.
* Pandoc runs in its own container, launched on demand.
* Want a newer Pandoc? Change one line: the image tag.
